---
import { getCollection } from 'astro:content';
import { FeaturedProducts } from './FeaturedProducts';

const products = await getCollection('products');
---

<FeaturedProducts products={products.map(p => p.data)} />
